interface:
  display_name: "Windows 驱动安装"
  short_description: "审计或安装缺失的驱动和固件"
  default_prompt: "Use $windows-install-drivers to audit or install missing Windows OEM firmware and hardware drivers."

policy:
  allow_implicit_invocation: true
