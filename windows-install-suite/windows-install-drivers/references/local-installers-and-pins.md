# Local Installers And Pinned Driver Packages

Programs listed here are exceptions to the "use Windows Update/OEM latest" rule.

Rules:

- Use local installers from `references/installer/` when a path is listed.
- Use pinned versions only when a version is listed here.
- For driver work, do not update working drivers just because a newer version exists.
- Put future manual driver installers under `references/installer/`.

## Local Installers

No local driver installers yet.

## Pinned Driver Packages

No pinned driver packages yet.
