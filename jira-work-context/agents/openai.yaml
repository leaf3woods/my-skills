interface:
  display_name: "Jira CLI"
  short_description: "Use Atlassian CLI syntax and safe command boundaries for Jira Cloud."
  default_prompt: "Use Atlassian CLI for Jira Cloud command discovery, read-only lookup, comments, and explicit safe mutations."
