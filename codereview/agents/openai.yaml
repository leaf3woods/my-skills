interface:
  display_name: "Code Review"
  short_description: "Review local Git changes for critical defects"
  default_prompt: "Use $codereview to review the current local Git changes for high-confidence critical defects without modifying files or running builds."
